<Window x:Class="MusicPlayerH.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Music Player H - Professional Music & Video Player" 
        Height="750" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🎵 Music Player H" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text=" | Professional Music & Video Player" 
                               FontSize="14" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" 
                             Fill="{Binding IsTrafficWatchConnected, Converter={StaticResource BoolToColorConverter}, FallbackValue={StaticResource DisconnectedBrush}}"
                             Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ConnectionStatus}" 
                               FontSize="12" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Now Playing Section -->
            <Border Grid.Column="0" 
                    Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="10" 
                    Padding="30"
                    VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Now Playing" 
                               FontSize="20" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource TextBrush}"
                               Margin="0,0,0,20"/>

                    <!-- Media Display Area -->
                    <Border Grid.Row="1" 
                            Background="#282828" 
                            CornerRadius="10"
                            Margin="0,0,0,20"
                            VerticalAlignment="Stretch">
                        <Grid>
                            <!-- Video View (hidden for audio) -->
                            <ContentControl x:Name="VideoView" 
                                            HorizontalContentAlignment="Stretch" 
                                            VerticalContentAlignment="Stretch"/>
                            
                            <!-- Album Art Placeholder -->
                            <StackPanel HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Visibility="Collapsed">
                                <TextBlock Text="🎵" FontSize="80" HorizontalAlignment="Center"/>
                                <TextBlock Text="No Media" 
                                           FontSize="18" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,10,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Track Info -->
                    <StackPanel Grid.Row="2">
                        <TextBlock Text="{Binding NowPlayingText}" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   Foreground="{StaticResource TextBrush}"
                                   TextWrapping="Wrap"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding CurrentTrack.Album, FallbackValue='Unknown Album'}" 
                                   FontSize="16" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Playlist Section -->
            <Border Grid.Column="1" 
                    Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="10" 
                    Padding="20"
                    Margin="20,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Playlist" 
                               FontSize="20" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource TextBrush}"/>

                    <!-- Toolbar -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,15,0,15">
                        <Button Content="➕ Add Files" 
                                Style="{StaticResource ModernButton}"
                                Command="{Binding AddFilesCommand}"
                                Margin="0,0,10,0"/>
                        <Button Content="📁 Add Folder" 
                                Style="{StaticResource ModernButton}"
                                Command="{Binding AddFolderCommand}"
                                Margin="0,0,10,0"/>
                        <Button Content="🗑️ Clear All" 
                                Style="{StaticResource ModernButton}"
                                Background="#FF5252"
                                Command="{Binding ClearPlaylistCommand}"/>
                    </StackPanel>

                    <!-- Playlist ListBox -->
                    <ListBox Grid.Row="2" 
                             ItemsSource="{Binding Playlist}"
                             SelectedItem="{Binding CurrentTrack}"
                             ItemContainerStyle="{StaticResource PlaylistItemStyle}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="🎵" 
                                               FontSize="16"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Artist}" 
                                                   FontSize="12" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Duration}" 
                                                   FontSize="12" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   Margin="0,0,15,0"/>
                                        <Button Content="❌" 
                                                FontSize="10"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Command="{Binding DataContext.RemoveSelectedTrackCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Player Controls -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="30,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Progress Bar -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{Binding CurrentTime}" 
                               FontSize="12" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Width="50"/>

                    <Slider Grid.Column="1" 
                            Minimum="0" 
                            Maximum="100" 
                            Value="{Binding Position, UpdateSourceTrigger=PropertyChanged}"
                            Margin="10,0"
                            Foreground="{StaticResource PrimaryBrush}"/>

                    <TextBlock Text="{Binding TotalTime}" 
                               Grid.Column="2" 
                               FontSize="12" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Width="50"/>
                </Grid>

                <!-- Control Buttons -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Volume Control -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="🔊" 
                                Style="{StaticResource IconButton}"
                                Command="{Binding ToggleMuteCommand}"
                                FontSize="18"/>
                        <Slider Width="100" 
                                Minimum="0" 
                                Maximum="100" 
                                Value="{Binding Volume}"
                                Margin="10,0,0,0"
                                Foreground="{StaticResource PrimaryBrush}"/>
                    </StackPanel>

                    <!-- Playback Controls -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="⏮️" 
                                Style="{StaticResource IconButton}"
                                Command="{Binding PlayPreviousCommand}"
                                FontSize="20"/>
                        <Button Content="⏹️" 
                                Style="{StaticResource IconButton}"
                                Command="{Binding StopCommand}"
                                FontSize="20"/>
                        <Button Content="{Binding IsPlaying, Converter={StaticResource BoolToPlayPauseConverter}, FallbackValue='▶️'}" 
                                Style="{StaticResource IconButton}"
                                Command="{Binding PlayPauseCommand}"
                                FontSize="28"
                                Width="60"
                                Height="60"
                                Background="{StaticResource PrimaryBrush}"/>
                        <Button Content="⏭️" 
                                Style="{StaticResource IconButton}"
                                Command="{Binding PlayNextCommand}"
                                FontSize="20"/>
                    </StackPanel>

                    <!-- Empty space for balance -->
                    <StackPanel Grid.Column="2"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
